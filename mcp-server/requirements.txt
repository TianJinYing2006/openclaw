# MCP Server 核心依赖
# 必须使用 mcp 1.29.x（LATEST_PROTOCOL_VERSION=2025-11-25，与 Spring AI 1.1.8 mcp-core 0.18.3 匹配）
# 1.2-1.6 不支持 streamable-http；1.15.x 协议版本仅到 2025-06-18，Java 客户端请求 2025-11-25 会被拒绝
# 1.29.0 streamable-http 有 Accept 头检查，通过 server.py 中 AcceptHeaderASGIMiddleware 修复
# 2.x 使用 v2 协议（MCPServer 替代 FastMCP），与 Spring AI 1.1.8 不兼容
mcp>=1.29.0,<2.0.0
httpx>=0.27.0
python-dotenv>=1.0.0
Pillow>=10.0.0
# uvicorn WebSocket 支持（SSE 连接升级必需，否则工具调用失败）
uvicorn[standard]>=0.30.0
websockets>=12.0
# hypercorn：支持 h2c（HTTP/2 cleartext），Java 21 HTTP 客户端默认发送 h2c 升级请求
# uvicorn 不支持 h2c，导致 POST body 丢失；hypercorn 解决此问题
hypercorn>=0.17.0

# 网页搜索（DuckDuckGo 免费方案，无需 API Key）
duckduckgo-search>=6.0.0

# 衣橱识别（百炼 qwen-vl-max 视觉模型，通过 OpenAI 兼容接口调用）
openai>=1.0.0

# 服装抠图（rembg 本地模型，首次运行自动下载约 170MB 模型文件）
rembg>=2.0.50
onnxruntime>=1.16.0

# 虚拟试衣（火山引擎 doubao-seedream-4.0，走 httpx 直调，无额外依赖）
