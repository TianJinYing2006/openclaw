# Git 与 IDEA 小组协作指南

当前远程仓库：<https://codeserver.youkeda.com/wangentong/7>

## 1. 先理解几个概念

- **工作区**：电脑里的项目文件，也就是 `D:\YKD-summer`。
- **本地仓库**：项目中的 `.git`，保存本地提交记录。
- **远程仓库**：学校 GitLab 上的小组仓库，IDEA 中叫 `origin`。
- **master**：小组公共主分支，保存已经确认的稳定代码。
- **wangentong**：个人开发分支，平时在这里修改和提交代码。

Git 中常说的“签入”一般是提交并推送；“签出”一般是切换到某个分支。

```text
修改文件 → 提交到本地 → 推送到个人分支
         → 合并请求 → 合并到 master
```

## 2. 每次开始工作

1. 点击 IDEA 顶部的分支名称，确认当前分支是 `wangentong`。
2. 点击 **Git → 拉取**，获取云端个人分支的最新内容。
3. 如果小组的 `master` 有更新：
   - 点击分支名称，切换到 `master`。
   - 点击 **Git → 拉取**。
   - 再切回 `wangentong`。
   - 点击分支名称，选择 `master`，点击 **将 master 合并到当前分支**。

这样个人分支既保留自己的代码，也包含最新的公共代码。

## 3. 修改、提交和推送

1. 在 `wangentong` 分支修改并保存代码。
2. 按 `Ctrl + K`，或者点击 **Git → 提交**。
3. 勾选这次需要提交的文件。
4. 双击文件，检查修改前后的差异。
5. 填写清楚的提交说明，例如：`增加用户登录功能`。
6. 点击 **提交并推送**。

如果只点击了“提交”，代码只保存在本地；还要按 `Ctrl + Shift + K` 执行推送，GitLab 才能看到。

## 4. 创建和切换分支

### 创建个人分支

1. 点击 IDEA 顶部的分支名称。
2. 点击 **新建分支**。
3. 输入分支名，例如 `zhangsan`。
4. 选择 **创建并签出**。
5. 第一次提交后执行推送，建立对应的远程分支。

### 切换分支

1. 切换前先提交当前修改，避免代码混乱或丢失。
2. 点击分支名称。
3. 选择目标分支。
4. 点击 **签出/Checkout**。

切换分支不是复制项目，而是在同一个仓库中切换不同版本的代码。

## 5. 把个人代码合并到 master

推荐在 GitLab 创建 Merge Request：

1. 先把 `wangentong` 的代码提交并推送。
2. 打开 GitLab 仓库，创建 **Merge Request**。
3. 来源分支选择 `wangentong`。
4. 目标分支选择 `master`。
5. 检查修改内容，确认后合并。
6. 合并完成后，在 IDEA 中切换到 `master` 并拉取。
7. 再切回 `wangentong`，把新的 `master` 合并到个人分支。

不要未经检查就直接向 `master` 推送代码。

## 6. 云端修改后的处理

如果在 GitLab 网页中修改了代码，云端会产生新提交。本地继续修改前必须：

1. 切换到被修改的分支。
2. 点击 **Git → 拉取**。
3. 确认更新完成后再继续写代码。

尽量不要同时在网页和本地修改同一个文件，否则容易产生冲突。

## 7. 解决合并冲突

1. IDEA 提示冲突时点击 **合并**。
2. 查看两边代码顶部的分支名称，不要只凭左右位置判断。
3. 在中间“结果”区域保留最终需要的代码。
4. 点击 **应用/完成合并**。
5. 运行项目或测试，确认代码正常。
6. 按 `Ctrl + K` 提交冲突处理结果，再推送。

不要直接选择“全部接受”，必须先看清楚双方修改。

## 8. 查看提交记录和代码差异

### 查看已经提交的记录

1. 按 `Alt + 9` 打开 Git 窗口。
2. 进入 **日志**。
3. 点击一条提交记录。
4. 双击右侧文件，查看修改前后的差异。

### 查看尚未提交的修改

1. 按 `Ctrl + K`。
2. 双击“更改”列表中的文件。

### 比较两个分支

点击分支名称，找到 `master`，选择 **与当前分支比较**。

## 9. `.gitignore` 注意事项

通常应该上传：

- Java 源代码和测试代码
- `pom.xml`
- 公共配置示例
- 项目说明文档

通常不应该上传：

- `.idea/`、`*.iml`：个人 IDEA 设置
- `target/`、`out/`、`*.class`：编译结果
- `.env`、密码、令牌和密钥：敏感信息
- 日志、缓存和临时文件

已经提交过的文件，后来加入 `.gitignore` 后不会自动从 Git 中消失，需要单独取消跟踪。

## 10. 日常操作口诀

```text
开工先拉取
平时在个人分支修改
提交前检查差异
提交后及时推送
通过 Merge Request 合并 master
master 更新后同步回个人分支
冲突解决后先测试再提交
```
