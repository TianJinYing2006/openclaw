# 项目周报（2026-07-13—2026-07-18）

## 本周概况

这周主要完成了微信 ClawBot 与 Java 项目的连接，并在基础消息收发之上接入大模型、图片和视频能力。项目从最开始的固定回复 Demo，逐步变成了一个能够处理文字、语音转写、图片和短视频的多模态机器人。

目前核心链路已经跑通：微信用户发消息，腾讯 iLink 服务把消息交给社区 Java SDK，Spring Boot 程序完成识别和处理，再通过 SDK 把结果回复到微信。本周共有 6 次 Git 提交；7 月 17～18 日新增的 AI、图片和视频代码仍在工作区，后续还需要整理后统一提交。

## 本周完成的工作

### 1. 跑通微信 iLink 通信

我先学习了 iLink 协议、JDK、SDK 和 Spring Boot 各自的作用，并确认 Java 侧使用社区 SDK `weixin-ilink-sdk:1.0.0`。随后完成二维码登录、Session 保存、长轮询收消息、游标更新和文本回复。

实际联调中已经成功扫码连接微信 ClawBot，连续接收并回复了三条文本消息。程序重启后还能读取本地 Session 自动恢复连接，不需要每次重新扫码。为了减少重复回复，还加入了消息 ID 去重和启动时旧消息过滤。

### 2. 从固定回复升级为大模型对话

在文本收发稳定后，我接入了 OpenAI Java SDK 和 Responses API。现在“帮助”“状态”“清空”仍由本地代码直接处理，其他普通问题交给大模型回答。

聊天记录按微信用户分别保存，每个用户最多保留最近 20 条消息，空闲两小时后自动清理。因此不同用户不会串话，用户继续追问时又能结合前面的内容回答。API Key、模型和服务地址都从环境变量读取，没有写进源码。

### 3. 完成图片理解和图片生成

用户发图片时，程序会通过 SDK 从腾讯 CDN 下载并解密，再把图片和问题一起交给视觉模型。当前限制一次最多 3 张、单张最大 5 MiB，避免请求过大。

图片生成使用“生图：描述”作为明确命令，并使用独立的图片 API 客户端。生成接口返回 PNG 后，Java 把原始图片字节交给 SDK，由 SDK 加密并上传腾讯 CDN，所以微信里可以直接显示图片，不需要用户再点击外部链接。

考虑到生图耗时较长，我把它放进独立线程池。这样生成图片时，普通文字消息仍然可以继续处理，不会全部堵在一个队列里。

### 4. 完成视频理解第一版

由于当前模型不能直接接收视频，我采用“下载解密视频 → FFmpeg 抽帧 → 多张图片交给模型 → 返回文字总结”的方案。

程序会先检查视频大小和时长，再用 `ffprobe` 读取真实时长，通过 FFmpeg 均匀抽取画面。按照最新要求，所有允许处理的视频固定抽取 10 帧，并在提示词中附上每帧时间点。当前限制为视频不超过 20 MiB、60 秒，每帧最长边 768 像素，十帧合计不超过 5 MiB。

视频使用独立的单线程队列，最多等待 5 个任务，不会阻塞普通文字和生图。真实 FFmpeg 测试已使用 6 秒合成视频成功抽出 10 张有效 JPEG。

### 5. 补充项目学习资料

本周还补充了源码中文注释、项目时序图、代码导读、AI 复刻指南和视频处理说明。文档重点解释了 Spring 启动、扫码登录、Session、游标、长轮询、消息分流、多轮对话、图片加解密和视频抽帧，方便后续复习、汇报和交接。

## 遇到的问题与处理

本周比较典型的问题有四个。

一是微信回复出现乱码，最后确认是 `.properties` 中文编码问题。默认中文改为 Unicode 转义并增加回归测试后，回复恢复正常。

二是 AI 提示认证失败。排查后发现 IntelliJ 环境变量曾把 Key 和多个开关写进同一个值，改成正确的分号分隔格式后恢复。也进一步明确了 Java 项目不会自动读取 CC Switch 的配置，必须单独设置环境变量。

三是图片生成和普通文字共用处理通道时，生图一慢，后面的文字也会等待。现在文字、生图和视频分别使用不同队列，慢任务之间不会互相完全阻塞。

四是 IDEA 启动时报 8080 端口占用。检查后发现旧的 Java 程序仍在后台运行，关闭旧进程并释放端口后恢复。这次错误与视频源码无关，日志中的 iLink 关闭异常只是启动失败后的附带现象。

## 当前验证结果

本周结束前重新运行了完整 Maven 测试，共 36 个测试，0 失败、0 错误、0 跳过，构建结果为 `BUILD SUCCESS`。已经验证的内容包括 Spring 上下文、消息类型、去重、Session、AI 异常处理、图片下载、回复分流、视频下载和真实 FFmpeg 十帧抽取。

微信扫码、文本收发和 Session 恢复已经真机验证；图片接口能够返回有效 PNG。视频目前完成了本地抽帧验证，但还需要发送一条真实微信短视频，完成腾讯 CDN 下载解密、十图模型分析和微信回复的最终联调。

## 下周计划

下周优先把当前未提交的 AI、图片和视频代码整理成一次清晰的 Git 提交，然后完成真实微信视频联调。视频理解稳定后，再把均匀十帧升级为“场景变化优先、不足时均匀补足十帧”，让模型看到的信息更有价值。

如果时间允许，再尝试从视频中提取音频并转成文字，使机器人不仅能看画面，也能理解视频里讲了什么。同时可以加入“视频状态”和“取消视频”命令，改善等待时间较长时的使用体验。

## 本周总结

这周最大的收获不是单纯增加了几个功能，而是逐渐理解了微信消息从腾讯服务器、iLink SDK、Java 业务代码到大模型，再返回微信的完整过程。项目现在已经具备比较完整的演示能力，下一阶段重点应该放在真实视频联调、关键帧质量和任务可靠性上，而不是继续盲目增加功能。
